**What changed**

**Why**

**Testing**

- [ ] `./gradlew testDebugUnitTest` passes
- [ ] JUnit test added under `app/src/test/java/` for the changed logic
- [ ] `scripts/test-*.sh` added, and it fails before the fix / passes after

<!-- UI-only changes with no logic change: note that instead of the boxes above. -->

**Related issue**

Closes #
